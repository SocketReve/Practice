/**
 * Created by Luca Reverberi - socketreve (thereve@gmail.com) on 29/10/14.
 */

angular.module("PracticeSimulator").controller("PracticeChartsController", function($scope, $modalInstance, Practice, PracticeSETOperations){
	/*Practice();
	var nodes = Practice.getNodes();
	var nodesArray = [];
	$scope.table = [];
	var powerSet;

	console.log(nodes);

	for(node in nodes) {
		if(nodes.hasOwnProperty(node)) {
			nodesArray.push(node);
		}
	}

	powerSet = PracticeSETOperations.PowerSet(nodesArray);
	console.log(powerSet);

	for(var i = 0; i < powerSet.length; i++) {
		if(i % 4 == 0) {
			$scope.table.push([]);
		}
		$scope.table[$scope.table.length-1].push(powerSet[i]);
	}
	console.log($scope.table);

	//$scope.powerSet = PracticeSETOperations();
	$scope.exit = function() {
		$modalInstance.dismiss('exit');
	}*/
});
